#!/usr/bin/env bash
# Configuring nginx and modifying the current page content.

sudo apt update
sudo apt install nginx
sudo listen 80 default_server
sudo echo "Hello World" > /var/www/html/index.nginx-debian.html
